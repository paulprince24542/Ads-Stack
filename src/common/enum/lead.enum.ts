export enum LeadStatus {
  NEW = 'new',
  CREATED = 'created',
  CONTACTED = 'contacted',
  PAID = 'paid',
  PROPOSAL_SENT = 'proposal_sent',
  CANCELLED = 'cancelled',
}

export enum LeadSource {
  WEBSITE = 'website',
  FACEBOOK = 'facebook',
  GOOGLE_ADS = 'google_ads',
  LINKEDIN = 'linkedin',
  WHATSAPP = 'whatsapp',
  REFERRAL = 'referral',
  PARTNER = 'partner',
  MANUAL = 'manual',
}
